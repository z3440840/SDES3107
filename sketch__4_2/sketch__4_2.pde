size(300,300);

int lineDist = 10;
line(lineDist,0,lineDist,height);

lineDist *= 2; 
line(lineDist,0,lineDist,height);

lineDist *= 2; 
line(lineDist,0,lineDist,height);

lineDist *= 2; 
line(lineDist,0,lineDist,height);

lineDist *= 2; 
line(lineDist,0,lineDist,height);

